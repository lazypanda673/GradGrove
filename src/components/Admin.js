function Admin() {
  return (
    <div style={{ padding: 32 }}>
      <h1 style={{ color: 'var(--color-warning)' }}>Admin Dashboard</h1>
      <p>Manage users, view analytics, and retrain AI model.</p>
      {/* Add more features as needed */}
    </div>
  );
}
export default Admin;
